---
version: "3"

services:
  certs:
    build: .
    environment:
      - DOMAIN=example.org
    volumes:
      - ./default.conf:/web/default.conf
      - certs:/etc/letsencrypt
      - nginx-conf:/sites-availables

volumes:
  certs:
    external: true
  nginx-conf:
    external: true
